info:
  name: New Game
  type: http
  seq: 6

http:
  method: POST
  url: "{{API_URL}}/game"
  body:
    type: json
    data: |-
      {
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
