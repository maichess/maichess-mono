info:
  name: Get Legal Moves for Square
  type: http
  seq: 18

http:
  method: GET
  url: "{{API_URL}}/game/moves/:sq"
  params:
    - name: sq
      value: e2
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
